# Add @lombok.Generated to generated members so JaCoCo excludes entity/DTO
# boilerplate (getters, setters, constructors) from coverage — leaving the
# metric to reflect hand-written logic only.
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
